/**
 * 空間レイアウト規約 v2「江戸の町、通りに面した奥の間」(全モジュール共有・変更時は全体影響に注意)。
 * 単位はメートル、現実準拠スケール。江戸町家の軒は低い(軒高2.2m)。
 *
 * 表通りは東西(X軸)に走る。東(+X)の突き当たりに神社の石段と鳥居、西(-X)の端に木戸。
 * プレイヤーの町家は通りの北側 x∈[-6,0]。店先(土間)→ 奥の間(畳・高床)→ 坪庭 と北に続く。
 * 南側の脇道(路地)に長屋の木戸・井戸端・物干し。
 */
export const LAYOUT = {
  /** 表通り(歩行可)。y=0 */
  STREET: { minX: -28, maxX: 30, minZ: -3, maxZ: 3 },
  /** 店先の土間(通り側)。三和土 y=0 */
  MISE: { minX: -6, maxX: 0, minZ: -8.5, maxZ: -3, floorY: 0 },
  /** 奥の間(畳・高床+0.35m)。v1書斎の後継 */
  OKUNOMA: { minX: -6, maxX: 0, minZ: -13, maxZ: -8.5, floorY: 0.35, ceilingY: 2.2 },
  /** 坪庭(奥の間の北) */
  TSUBONIWA: { minX: -6, maxX: 0, minZ: -16, maxZ: -13, groundY: 0 },
  /** 南側の路地(井戸端・物干し・長屋木戸)。突き当たりで行き止まり */
  ALLEY: { minX: 6, maxX: 9.5, minZ: 3, maxZ: 14 },
  /** 奥の間⇔店先の障子の通り */
  SHOJI_Z: -8.5,
  /** 軒高(町家の「圧の低さ」— 妥協しない) */
  EAVE_Y: 2.2,
  /** 通り東端の先(結界の外): 神社の石段・鳥居の中心X */
  SHRINE_X: 36,
  /** 通り西端の木戸(夜は閉まる)の中心X */
  KIDO_X: -28,
  /** 蕎麦屋台の位置(通り南縁) */
  YATAI: { x: 10, z: 2.2 },
  /** 井戸(路地の井戸端) */
  WELL: { x: 8.2, z: 10 },
  /** 歩行/テレポートの外周クランプ(desktop用の粗い境界) */
  BOUNDS: { minX: -29, maxX: 31, minZ: -17, maxZ: 15 },
  /** スポーン(奥の間の中央、南=障子・通り方向を向く) */
  SPAWN: { x: -3, y: 0.35, z: -10.5, yaw: Math.PI },
  /** 目線高さ(デスクトップフォールバック用) */
  EYE_HEIGHT: 1.6,
} as const
