import type { AppContext, WorldRefs } from '../core/types'

/**
 * 家具(文机・本棚・掛け軸・行灯・デスクランプ・ラジオ・座布団)を構築する。
 * TODO(sonnet-world): 本実装。
 */
export function buildFurniture(_ctx: AppContext): Partial<WorldRefs> {
  return {}
}
